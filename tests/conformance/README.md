# Conformance Tests

## IEP Pack Fixture

Canonical IEP pack fixture is **pending import**.

When available, place at:

`tests/conformance/fixtures/iep.pack.yaml`

Provenance: `hand-authored-pre-forge`

Use ONLY as test data. Do not copy IEP semantics into generic code.
