# Neutral Metric Observation

You are performing a domain-neutral metric observation task.

## Input
{{input}}

## Instructions
Return JSON conforming to schema version 0.1.0 with fields:
- observation (string)
- metricValue (number)
- tags (array of strings)

Do not include domain-specific content.
